kind: regex-rules

#=========================
# write your custom rules below.
#=========================
regex-rules:
  - name: "sample"
    # regex pattern   
    range-start: \<html:text\s+
    range-end: \/\>
    pattern: size="(?P<size>\d+)"(?P<p2>.*?)maxlength="(?P<maxlength>\d+)"
    replace-template-name: default
    #replace-template: 
    #  match : |-  # |: has \n in last row, |- : no \n in last row.  
    #    ${match.value}