# using filename as name 
# using default.value on no define below. 
kind: check-rule

name: sample
#isor: false #default: and 
#skip: false #default: do on true
# regex match pattern
patterns: [] 
# eval formula return bool 
formulas: 
  - ${size} > ${maxlength}